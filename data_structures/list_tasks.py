"""Здачи по спискам."""
import sys
from pathlib import Path

if __name__ == '__main__':
    sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from functions.func import get_index, select_value

# Дан список букв из названия фильма:

# python
# movie_name_sequence = ['Д', 'ж', 'о', 'н', 'н', 'и', ' ', 'М', 'н', 'е',
# 'м', 'о', 'н', 'и', 'к']
# Напишите код, который выведет на экран слово «жми», составленное из
# элементов этого списка.
# Используйте только обращение по индексам и функцию print(). Каждая буква —
# отдельный вызов print()
# (чтобы каждая буква оказалась на новой строке).

# Ожидаемый вывод:
# ж
# м
# и

movie_name_sequence = ['Д', 'ж', 'о', 'н', 'н', 'и', ' ', 'М', 'н', 'е', 'м',
                       'о', 'н', 'и', 'к']

first_letter, second_letter, third_letter = [
    movie_name_sequence[get_index(movie_name_sequence, 'ж')],
    movie_name_sequence[get_index(movie_name_sequence, 'м')],
    movie_name_sequence[get_index(movie_name_sequence, 'и')]
    ]
print(first_letter)
print(second_letter)
print(third_letter)

# В списке apples_by_day хранятся данные об урожае яблок за 10 дней:
# apples_by_day = [61, 58, 56, 34, 67, 50, 74, 64, 78, 69]
# Распакуйте этот список в отдельные переменные, дав им осмысленные имена
# (например, day1, day2, … или mon, tue, …).
# Затем вычислите и напечатайте сумму яблок за первые три дня и сумму яблок за
# последние три дня, используя только полученные переменные (без обращения к
# исходному списку).

apples_by_day = [61, 58, 56, 34, 67, 50, 74, 64, 78, 69]
day1, day2, day3, day4, day5, day6, day7, day8, day9, day10 = apples_by_day
print(day1 + day2 + day3)
print(day8 + day9 + day10)

# Создайте список basket с тремя овощами: 'Помидоры', 'Огурцы', 'Кабачки'.
# Выполните последовательно следующие действия:

# Добавьте в конец списка 'Баклажаны'.

# Вставьте на вторую позицию (индекс 1) 'Перец'.

# Удалите из списка 'Огурцы' (используйте метод remove).

# Удалите последний элемент списка и сохраните его в переменную
# last_vegetable (используйте pop).

# После каждого шага печатайте список, чтобы видеть изменения. В конце
# выведите значение переменной last_vegetable.

# Ожидаемый итоговый список: ['Помидоры', 'Перец', 'Кабачки']
# В переменной last_vegetable: 'Баклажаны'

basket = ['Помидоры', 'Огурцы', 'Кабачки']
print(basket)
basket.append('Баклажаны')
print(basket)
basket.insert(1, 'Перец')
print(basket)
basket.remove('Огурцы')
print(basket)
last_vegetable = basket.pop(-1)
print(basket)
print(last_vegetable)

# Дан список урожайности:
# yields = [10, 12, 15, 10, 8, 10]
# Создайте настоящую копию этого списка в переменной true_copy с помощью метода copy.
# Создайте переменную fake_copy и присвойте ей значение yields.
# Отсортируйте исходный список yields по возрастанию.
# Выведите на экран yields, true_copy и fake_copy.
# Объясните (в комментарии) почему вывод true_copy и fake_copy различается.

yields = [10, 12, 15, 10, 8, 10]
true_copy = yields.copy()
fake_copy = yields
yields.sort()
print(yields)
print(true_copy)
print(fake_copy)
# Отличие заключается в том что данные списки стали разными сущностями в тот
# момент когда
# мы создали копию исходного списка через метод copy


# Дан список урожайности овощей за неделю:
# harvest = [5, 12, 8, 12, 15, 8, 10]
# Выполните следующие действия и зафиксируйте результаты:
# Сколько раз в списке встречается значение 12?
# Сохраните ответ в переменную count_12.
# На каком месте (индекс) впервые встречается значение 8?
# Сохраните в first_8_index.
# Отсортируйте список по возрастанию, а затем разверните его
# (сделайте сортировку по убыванию,
# используя комбинацию sort и reverse).
# Найдите и сохраните в переменную last_harvest последний элемент
# отсортированного
# по убыванию списка (то есть самый маленький урожай), используя
# pop без аргументов.
# Выведите все полученные переменные: count_12, first_8_index,
# отсортированный по убыванию список и last_harvest.
# Ожидаемый результат:
# count_12 → 2
# first_8_index → 2
# Отсортированный по убыванию список → [15, 12, 12, 10, 8, 8, 5]
# last_harvest → 5

harvest = [5, 12, 8, 12, 15, 8, 10]
count_12 = harvest.count(12)
print(count_12)
first_8_index = harvest.index(8)
print(first_8_index)
harvest.sort()
print(harvest)
harvest.reverse()
print(harvest)
last_harvest = harvest.pop()
print(last_harvest)

# Дан кортеж с урожаем яблок за десять дней:
# apples = (61, 58, 56, 34, 67, 50, 74, 64, 78, 69)
# Распакуйте кортеж в переменные day1, day2, ..., day10
# (осмысленные имена).
# Используя только переменные, вычислите и напечатайте
# сумму урожая за первые три дня.
# Используя индексацию исходного кортежа, выведите на
# экран значение шестого дня (индекс 5).
# Ожидаемый вывод:
# text
# 175
# 50
apples = (61, 58, 56, 34, 67, 50, 74, 64, 78, 69)
apples_day_1, apples_day_2, apples_day_3, apples_day_4, apples_day_5, apples_day_6, apples_day_7, apples_day_8, apples_day_9, apples_day_10 = apples  
print(apples_day_1 + apples_day_2 + apples_day_3)
print(apples[5])

# Напишите функцию describe_harvest(*crops), которая принимает
# произвольное количество названий культур (строк) и
# возвращает кортеж из двух элементов:
# строку вида "Урожай собран: яблоки, груши, сливы"
# (все переданные культуры через запятую и пробел),
# количество переданных культур (целое число).
# Вызовите функцию с аргументами 'яблоки', 'груши', 'сливы'.
# Распакуйте возвращаемый кортеж в переменные 
# description и count, выведите их на отдельных строках.
# Ожидаемый вывод:
# Урожай собран: яблоки, груши, сливы
# 3


def describe_harvest(*crops):
    """Функция describe_harvest."""
    return f'Урожай собран: {crops}', len(crops)


description, count = describe_harvest('яблоки', 'груши', 'сливы')
print(description)
print(count)

# Дан кортеж урожайности:
# yields = (120, 85, 150, 90, 200, 110, 95)
# Создайте новый кортеж high_yields_sorted, который
# содержит все значения из yields, превышающие
# 100, отсортированные по убыванию. Исходный кортеж yields должен остаться неизменным.
# Подсказка:
# Сначала получите новый кортеж из
# подходящих элементов (например, с помощью tuple(x for x in yields if x > 100)).
# Затем примените функцию sorted() с
# параметром reverse=True и результат преобразуйте в кортеж.
# Выведите исходный кортеж и получившийся high_yields_sorted.
# Ожидаемый вывод:
# (120, 85, 150, 90, 200, 110, 95)
# (200, 150, 120, 110)

yields_tuple = (120, 85, 150, 90, 200, 110, 95)

tuple_with_one_hundred = tuple(select_value(yields_tuple, 100))
print(yields_tuple)
print(tuple_with_one_hundred)


# Дана строка:
# garden_str = 'Кабачки полезны для здоровья.'
# Напишите код, который только с помощью индексации
# и функции print() выведет на экран слово «зал», по одной букве
# на строке. Используйте положительные индексы для всех букв.
# (Напоминание: пробел — это тоже символ со своим индексом.)
# Ожидаемый вывод:
# з
# а
# л

garden_str = 'Кабачки полезны для здоровья.'
print(garden_str[garden_str.find('з')])
print(garden_str[garden_str.find('а')])
print(garden_str[garden_str.find('л')])


# Дана строка с «грязным» текстом:
# raw_report = '  сЕГОДНЯ Урожай СОСТАВИЛ 150 кг.  '
# Выполните последовательно:
# Удалите лишние пробелы в начале и конце строки (метод strip).
# Приведите строку к формату, в котором каждое слово начинается
# с заглавной буквы, а остальные буквы — строчные (метод title).
# Замените слово Сегодня на Вчера (метод replace).
# Сохраните итоговую строку в переменную clean_report.
# Выведите с помощью f-строки: Обработанный отчёт: {clean_report}. Длина
# строки: {длина_строки} символов.
# Ожидаемый вывод:
# Обработанный отчёт: Вчера Урожай Составил 150 Кг. Длина строки: 33 символов.

raw_report = '  сЕГОДНЯ Урожай СОСТАВИЛ 150 кг.  '
raw_report = raw_report.strip(' ')
raw_report = raw_report.title()
clean_report = raw_report.replace('Сегодня', 'Вчера')
print(f'Обработанный отчёт: {clean_report} Длина строки: {len(clean_report)} '
      'символов.')

# Даны переменные:
# crop = 'томаты'
# harvest_kg = 145.678
# price_per_kg = 85.5
# plan_kg = 200
# Составьте и напечатайте одну f-строку, которая выводит:
# Название культуры.
# Фактический урожай в кг, округлённый до двух знаков после запятой.
# Вычисленную общую стоимость урожая (урожай × цена),
# округлённую до двух знаков после запятой.
# Процент выполнения плана (урожай / план), выведенный
# в формате процентов с одним десятичным знаком.
# Ожидаемый вывод:
# Урожай томаты: 145.68 кг. Стоимость: 12455.47 руб. Выполнение плана: 72.8%.

crop = 'томаты'
harvest_kg = 145.678
price_per_kg = 85.5
plan_kg = 200
total_harvest_price = harvest_kg * price_per_kg
harvest_target = harvest_kg / plan_kg
print(f'Урожай {crop}: {harvest_kg:.2f} кг. Стоимость: '
      f'{total_harvest_price:.2f} руб. Выполнение плана: {harvest_target:.1%}.')

# После посадки огурцов первый полив нужно 
# сделать на 3-й день, а затем поливать каждые 4 дня.
# Напишите код, который создаёт диапазон water_days для первых 8 поливов.
# Преобразуйте диапазон в список и выведите его.
# Ожидаемый вывод:
# [3, 7, 11, 15, 19, 23, 27, 31]

water_days = list(range(3, 8*4+3, 4))
print(water_days)

# Дана строка:
# text = 'Python'
# С помощью диапазона индексов получите доступ к символам, стоящим
# на чётных позициях
# (индексы 0, 2, 4, ...), и составьте из них новую строку. Выведите эту строку.
# Ожидаемый вывод:
# Pto
text_python = 'Python'
new_version_pyton_range = list(range(0, 5, 2))
new_string = ''
for i in new_version_pyton_range:
    new_string += text_python[i]
print(new_string)

# Создайте диапазон:
# numbers = range(15, 85, 11)
# Не преобразуя его полностью в список на первом этапе, выведите:
# Первый элемент диапазона (по индексу 0).
# Последний элемент (по отрицательному индексу –1).
# Количество элементов в диапазоне (преобразовав его
# на время в список или кортеж, чтобы узнать длину).
# Затем преобразуйте numbers в кортеж и
# вычислите сумму всех его элементов. Выведите сумму.
# Ожидаемый вывод:
# Первый: 15
# Последний: 81
# Количество элементов: 7
# Сумма: 336

numbers = range(15, 85, 11)
print(f'Первый: {numbers[0]}')
print(f'Последний: {numbers[-1]}')
print(f'Количество элементов: {len(numbers)}')
sum = 0
for i in numbers:
    sum += i
print(f'Сумма: {sum}')
